# トリハル 公式サイト — プロジェクト指示

和歌山市南大工町の焼き鳥・鰻の**持ち帰り専門店**「有限会社 トリハル」の公式サイト。
ルートの `c:\projects\CLAUDE.md`（Elite Web Agency Master System）に加えて、以下を必ず守ること。

---

## 絶対に守る店舗情報

| 項目 | 値 |
| --- | --- |
| 表示名 | トリハル |
| 会社名 | 有限会社 トリハル |
| 住所 | 〒640-8032 和歌山県和歌山市南大工町20 |
| 目印 | フォルテワジマの西隣 |
| 電話 | 073-422-4088 |
| 営業時間 | 8:30〜17:00 |
| 定休日 | 毎週火曜日 |
| 業態 | 焼き鳥・鰻の持ち帰り専門店（店内に客席はない） |
| Instagram | https://www.instagram.com/toriharu_unagi/ |
| 店舗数 | 南大工町の1店舗のみ |

これらは `data/shop.ts` が唯一の正。**他のファイルに直接書かない。**

旧サイト（toriharu.mods.jp）には古い情報が残っている。営業時間・価格は必ず上の表と
`data/menu.ts` を優先すること。旧サイトの表記（萬町など）は使わない。

---

## 書いてよいこと／書いてはいけないこと

`data/verified-facts.json` がすべて。ここに無いことは書かない。

特に禁止：

- 店内飲食・イートイン・座席・宴会があるかのような記述
- オンラインショップ・通販サイト・ネット注文があるかのような記述
- `data/menu.ts` に無い価格、リストに無い商品名
- 創業年の西暦、「〇代目」（「明治創業」「120年以上」のみ可）
- 鰻や食材の健康効果・効能の断定
- 口コミ、受賞歴、メディア掲載、スタッフ名、仕入れ先
- その年の土用の丑の日など、年によって変わる日付のハードコード
- 「究極」「絶品」「最高品質」などの誇大表現

未確認の情報は `null` にして画面から落とす。埋めない。

---

## デザインの方針

テーマは **「受け継ぐ火、受け継ぐ味。」**

- 写真を大胆に（全幅・縦長・大きなクローズアップ）。小さなカードに閉じ込めない
- 左右非対称のレイアウト、余白、縦書き、大きな明朝の見出し
- 赤（`tan` #a52a20）はアクセント。主役は写真と余白
- 高級料亭にしすぎない。「和歌山の老舗商店 × 昔ながらの焼き場 × 現代のエディトリアル」

禁止：角丸カードの羅列、均一な3カラム、意味のないグラデーション、
ガラスモーフィズム、丸アイコンの列、巨大な数字を並べただけのセクション、
「選ばれる3つの理由」的な定型セクション。

**ヒーローにCTAボタンを置かない。**

### 実装上の決まり

- カラーとフォントは `app/globals.css` の `@theme` に定義。個別に色を書かない
- カスタムクラスは必ず `@layer components` の中に書く（Tailwind v4 では素で書くと
  ユーティリティを打ち消す）
- スクロール出現は `components/ui/Reveal.tsx`。state ではなく `data-visible` を
  直接書き換えている（再レンダリングを起こさないため）
- 2カラムでテキスト側が短くなる場合は `lg:sticky lg:top-28 lg:self-start` を付ける。
  body に `overflow-x: hidden` を足すと sticky が壊れるので入れない
- `next/image` のラッパーは `components/ui/Photo.tsx`。`fill` を使うときは
  親に `relative` と高さが必要
- `data/photos.ts` の `kind` は内部管理用（画面には出さない）。実写が用意でき次第、
  `kind: 'image'` のものから差し替える

---

## 検証

変更したら必ず通すこと。

```bash
npx tsc --noEmit && npx eslint . && NEXT_PUBLIC_SITE_URL=https://example.com npm run build
```

加えて確認する項目：

- 各ページ h1 は1つ
- 画像切れ・横スクロールなし（PC 1440px / SP 390px）
- コントラスト AA（小さい文字 4.5:1、大きい文字 3:1）
- 営業時間が 8:30〜17:00 になっている
- 「持ち帰り専門」であることが誤解されない
